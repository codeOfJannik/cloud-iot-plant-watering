stages:
    - test

test:
    stage: test
    script:
        - echo "Testing"
        - apk update
        - apk add --update-cache python3 py-pip
        - pip3 install pip AWSIoTPythonSDK
        - python3 -m unittest discover -s tests/
