stages:
    - test

test:
    stage: test
    script:
        - echo "Testing"
        - apk update
        - apk add --update-cache python python-dev py-pip
        - python -m unittest discover -t ./tests
